# DNS Inspector

## Build
Create a small CLI that takes a domain name and prints:
- A records
- AAAA records
- CNAME chain if present
- TTL if your language/runtime exposes it

## Why this matters
It turns DNS from an abstract concept into a thing you can inspect before making an HTTP request.
