# Raw HTTP Client

## Build
Create a tiny client that:
- Opens a TCP connection to a host
- Sends a raw HTTP GET request
- Prints the full response status line, headers, and body
- Repeats the same request with different headers and methods

## Why this matters
It shows what curl, browsers, and frameworks are doing behind the scenes.
